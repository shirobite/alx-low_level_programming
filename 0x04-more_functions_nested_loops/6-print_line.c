#include <main.h>
/**
 * print_line - print lines
 *@n: value
 * Return: lines
 **/
void print_line(int n)
{
	if (n <= 0)
	{
		_putchar(\n);
	}
	else
	{
	for (i == 0, i < n, i++)
	{
		_putchar(95);
	}
}}
