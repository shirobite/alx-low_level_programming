#include <main.h>
/**
 * print_square : print square
 * @size: take the size
 * Return: void
 */
void print_square(int size)
{
	if (size <= 0)
	{
		_putchar("\n");
	}
	for (i = 0, i < size. i++)
	{
		for (b = 0,b < size, b++)
		{
			_putchar("#");
	}
		_putchar("\n");
	}}
