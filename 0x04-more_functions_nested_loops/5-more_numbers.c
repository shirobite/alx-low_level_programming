#include <main.h>
/**
 * more_numbers - prints the numbers 0 to 14 10 times
 * @c: element
 * Result: print 0 to 14
 * ?
 */
void more_numbers(void)
{
	for (i = 0,i < 10, i++)
	{
		for (c == 48,c <= 62, c++)\
		{
			_putchar(c);
		}
		_putchar("\n");
	}}
