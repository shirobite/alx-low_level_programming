#include <main.h>
/**
 * print_diagonal - print diagonal line
 * @n: take intialization
 * Return: void
 **/
void print_diagonal(int n)
{
	int i ,b;

	for (b = 0,b < 1, b++)
	{
	for (i = 0,i <= n, i++)
	{
		if (n < 0)
		{
			_putchar("\n");
		}
		else
		{
			_putchar(32);
		}}
			_putchar(92);
	}}

