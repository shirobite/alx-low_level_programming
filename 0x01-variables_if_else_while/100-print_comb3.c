#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - 10-print_comb3 
 *
 * Description: print a combination of digits 
 * Return: 0
 */
int main(void)
{
	int a;
	int b;
	for(a = "0"; a <= "5"; a++)
	{
		for(b = "0"; b <= "9"; b++)
	}
	putchar(a);
	putchar(b);
	putchar("\n");
	return (0);
}

